extends ../layout.pug

block content
  //- Form Container
  div(class='w-full max-w-3xl bg-white shadow-md rounded-lg p-6')
    h1(class='text-2xl font-bold mb-4') Update Category

    form(method='post' enctype='multipart/form-data')
      //- Category Title
      div(class='mb-4')
        label(for='title' class='block text-gray-700 font-bold mb-2') Title
        input(type='text', id='title', name='title', required, value=category.title, class='w-full p-2 border border-gray-300 rounded')

      //- Category Image
      div(class='mb-4')
        label(for='img_url' class='block text-gray-700 font-bold mb-2') Category Image
        input(type='file', id='img_url', name='img_url', accept='image/*', class='w-full p-2 border border-gray-300 rounded')
        //- Show the existing image
        if category.img_url
          div(class='mt-2')
            img(src=category.img_url alt=category.title class='w-16 h-16 object-cover rounded-lg')

      //- Description
      div(class='mb-4')
        label(for='desc' class='block text-gray-700 font-bold mb-2') Description
        textarea(id='desc', name='desc', rows='4', required, class='w-full p-2 border border-gray-300 rounded')= category.desc

      //- Submit Button
      div(class='flex justify-end')
        button(type='submit', class='bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600') Update
